using UnityEngine;

public class RoomsGeneration : MonoBehaviour
{
    
    public GameObject[] roomPrefabs; // Tes différentes salles
    public int numberOfRooms = 10; // Nombre total de salles à générer

    private Vector3 nextRoomPosition = Vector3.zero; // Position pour la prochaine salle

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
